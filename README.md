Get a key from https://rapidapi.com/winevybe/api/beer9/

Copy the file appsettings.example.json to appsettings.json and fill in the required information

Import the packages with dotnet restore

Apply the migrations with dotnet ef database update

Build the program with dotnet build

Run the program with dotnet run